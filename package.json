{
  "name": "portal-facturas",
  // otras propiedades...
  "homepage": "https://IvanMcNish.github.io/PortalFacturas/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"  // o la última versión
  }
}
